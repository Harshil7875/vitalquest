# Environment secrets — NEVER commit
.env
*.env

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
venv/
.venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
coverage.xml

# Alembic versions are tracked in git — do not ignore

# ─── Frontend (Expo / React Native) ──────────────────────────────────────────

# Dependencies
frontend/node_modules/

# Expo
frontend/.expo/
frontend/dist/
frontend/web-build/

# React Native build artifacts
frontend/android/
frontend/ios/

# Metro bundler cache
frontend/.metro-health-check*

# TypeScript build output
frontend/*.tsbuildinfo

# Expo local secrets (never contains real secrets but keep out of git)
frontend/.env.local

# ─── IDE ─────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ─── macOS ───────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
